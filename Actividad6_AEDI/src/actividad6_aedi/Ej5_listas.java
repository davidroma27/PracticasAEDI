package actividad6_aedi;
import lista.*;

/**
 * @author david
 * Escribe un método que dada una lista L devuelva otra lista R conteniendo los
 * elementos repetidos de L. Por ejemplo, si L almacena los valores 5, 2, 7, 2, 5, 5, 1, debe
 * construirse una lista R con los valores 5, 2. Si en L no hay elementos repetidos, R será
 * la lista vacía
 */
public class Ej5_listas {
    
    public Lista<Integer> devuelveRep(Lista<Integer> L){
        Lista<Integer> R = new ListaEnlazada();
        int veces = 0;
        
        //No se como implementar el código que identifica a los elementos que se repiten
        
    }
}
